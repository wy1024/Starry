/*
Deployment script for starry

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "starry"
:setvar DefaultFilePrefix "starry"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
/*
The column [dbo].[PeopleDetails].[crawled_times] on table [dbo].[PeopleDetails] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[PeopleDetails].[owner_id] on table [dbo].[PeopleDetails] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[PeopleDetails])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Room].[category] on table [dbo].[Room] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Room].[gift] on table [dbo].[Room] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Room].[online] on table [dbo].[Room] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Room].[timestamp] on table [dbo].[Room] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Room])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Creating [dbo].[PeopleDetailsType]...';


GO
CREATE TYPE [dbo].[PeopleDetailsType] AS TABLE (
    [weight]        NVARCHAR (50)  NULL,
    [avg_audience]  INT            NULL,
    [analysis]      NVARCHAR (MAX) NULL,
    [owner_id]      INT            NOT NULL,
    [crawled_times] NCHAR (10)     NOT NULL);


GO
PRINT N'Creating [dbo].[PeopleEntityType]...';


GO
CREATE TYPE [dbo].[PeopleEntityType] AS TABLE (
    [owner_id]   INT           NOT NULL,
    [owner_name] NVARCHAR (50) NOT NULL,
    [verified]   SMALLINT      DEFAULT 0 NOT NULL);


GO
PRINT N'Altering [dbo].[PeopleDetails]...';


GO
ALTER TABLE [dbo].[PeopleDetails]
    ADD [owner_id]      INT        NOT NULL,
        [crawled_times] NCHAR (10) NOT NULL;


GO
PRINT N'Altering [dbo].[Room]...';


GO
ALTER TABLE [dbo].[Room]
    ADD [online]    INT            NOT NULL,
        [category]  NVARCHAR (50)  NOT NULL,
        [gift]      NVARCHAR (MAX) NOT NULL,
        [timestamp] DATETIME       NOT NULL;


GO
PRINT N'Creating [dbo].[CK_PeopleDetails_People]...';


GO
ALTER TABLE [dbo].[PeopleDetails] WITH NOCHECK
    ADD CONSTRAINT [CK_PeopleDetails_People] FOREIGN KEY ([owner_id]) REFERENCES [dbo].[People] ([owner_id]);


GO
PRINT N'Creating [dbo].[People_Upsert]...';


GO

CREATE PROCEDURE [dbo].[People_Upsert] @tempEntities AS PeopleEntityType READONLY
AS
	BEGIN

		MERGE INTO [dbo].[People] AS target
		USING @tempEntities AS source
		ON (target.owner_id = source.owner_id)
		WHEN MATCHED THEN
			UPDATE SET 
					target.verified = source.verified
		WHEN NOT MATCHED THEN
			INSERT (owner_id, owner_name, verified)
			VALUES (source.owner_id,
					source.owner_name,
					source.verified
				);	
	END;
GO
PRINT N'Creating [dbo].[PeopleDetails_Upsert]...';


GO

CREATE PROCEDURE [dbo].[PeopleDetails_Upsert] @tempEntities AS PeopleDetailsType READONLY
AS
	BEGIN

		MERGE INTO [dbo].[PeopleDetails] AS target
		USING @tempEntities AS source
		ON (target.owner_id = source.owner_id)
		WHEN MATCHED THEN
			UPDATE SET 
					target.avg_audience = source.avg_audience,
					target.crawled_times = source.crawled_times,
					target.weight = source.weight,
					target.analysis = source.analysis
		WHEN NOT MATCHED THEN
			INSERT (weight, avg_audience, analysis, owner_id, crawled_times)
			VALUES (source.weight,
					source.avg_audience,
					source.analysis,
					source.owner_id,
					source.crawled_times
				);	
	END;
GO
PRINT N'Creating [dbo].[Room_Insert]...';


GO
CREATE PROCEDURE [dbo].[Room_Insert]
	@room_id INT NOT NULL,
    @owner_id INT NOT NULL,
	@online INT NOT NULL, 
    @category NVARCHAR(50) NOT NULL, 
    @gift NVARCHAR(MAX) NOT NULL, 
    @timestamp DATETIME NOT NULL
AS
	INSERT INTO [dbo].[Room]
	VALUES (@room_id, @owner_id, @online, @category, @gift, @timestamp);
RETURN 0
GO
